- flash.each do |key, message|
  div role="alert" class = "alert alert-#{flash_to_alert(key)} alert-dismissible fade show mt-3"
    strong = sanitize(message)
    button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"
